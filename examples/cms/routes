# Serve HTML, CSS, JS files
/           renderer/render.sh 	   exec(text:index) header(content-type:text/html)
/styles/*   css/*          	   header(content-type:text/css)
/images/*   media/*  		   header(content-type:image/jpg)

# Markdown Renderer
/content/*	 renderer/render.sh	 exec header(content-type:text/html)

# Tells you what the date is
/api/date	api/date	exec

/test	echo	exec(test)
